import { ResponderSugerenciaComponent } from './responder-sugerencia.component'

describe('ResponderSugerenciaComponent', () => {
  it('should mount', () => {
    cy.mount(ResponderSugerenciaComponent)
  })
})